#include <stdio.h> // Incluye la biblioteca estándar de entrada y salida

int main() { // Función principal
    long estudiantes = 30; // Declara una variable long 'poblacion' y le asigna un valor grande
    printf("La capcidad en el salon es de: %ld\n", estudiantes); // Imprime el valor de la variable 'poblacion'
    return 0; // Indica que el programa terminó correctamente
}
